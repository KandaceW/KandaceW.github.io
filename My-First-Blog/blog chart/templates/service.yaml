apiVersion: v1
kind: Service
metadata:
  name: {{ template "blog chart.fullname" . }}
  labels:
    app: {{ template "blog chart.name" . }}
    chart: {{ template "blog chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "blog chart.name" . }}
    release: {{ .Release.Name }}
